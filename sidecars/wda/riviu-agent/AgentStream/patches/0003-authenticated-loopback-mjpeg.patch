diff --git a/WebDriverAgentLib/Commands/FBSessionCommands.m b/WebDriverAgentLib/Commands/FBSessionCommands.m
index 90a9b8b91d72906573dab29636096ce3ad944e6d..8cc800755e83f88dedb9adb7907569efe0d0a820 100644
--- a/WebDriverAgentLib/Commands/FBSessionCommands.m
+++ b/WebDriverAgentLib/Commands/FBSessionCommands.m
@@ -18,6 +18,7 @@
 #import "FBSettings.h"
 #import "FBSettingsHandler.h"
 #import "FBRuntimeUtils.h"
+#import "FBWebServer.h"
 #import "FBXCodeCompatibility.h"
 #import "XCUIApplication+FBHelpers.h"
 #import "XCUIApplication+FBQuiescence.h"
@@ -223,13 +224,16 @@
 
 + (NSDictionary<NSString *, id> *)riviuAgentIdentity
 {
+  BOOL streamReady = FBRiviuAgentStreamIsReady();
   return @{
     @"agentVersion": @"0.1.0",
     @"protocolVersion": @2,
-    @"features": @[@"stream", @"tap", @"swipe", @"clipboard"],
+    @"features": streamReady
+      ? @[@"stream", @"tap", @"swipe", @"clipboard"]
+      : @[@"tap", @"swipe", @"clipboard"],
     @"logicalWidth": @375,
     @"logicalHeight": @667,
-    @"state": @"ready",
+    @"state": streamReady ? @"ready" : @"degraded",
   };
 }
 
diff --git a/WebDriverAgentLib/Routing/FBTCPSocket.h b/WebDriverAgentLib/Routing/FBTCPSocket.h
index 72947e2a0532d4dadb8b0b46d98390f74704fe0a..a408be29bc973cf77defc4ed09e4e746bfead652 100644
--- a/WebDriverAgentLib/Routing/FBTCPSocket.h
+++ b/WebDriverAgentLib/Routing/FBTCPSocket.h
@@ -23,8 +23,9 @@ NS_ASSUME_NONNULL_BEGIN
  The callback which is fired when the TCP server receives a data from a connected client
 
  @param client The client, which sent the data
+ @param data The bounded request header data read from the client
 */
-- (void)didClientSendData:(GCDAsyncSocket *)client;
+- (void)didClient:(GCDAsyncSocket *)client sendData:(NSData *)data;
 
 /**
  The callback which is fired when TCP client disconnects
diff --git a/WebDriverAgentLib/Routing/FBTCPSocket.m b/WebDriverAgentLib/Routing/FBTCPSocket.m
index e23876d83fc6cdb8ead38493b30666405c911a86..ca64bcd4b3f780e41cedffa5b4d861f6524493ce 100644
--- a/WebDriverAgentLib/Routing/FBTCPSocket.m
+++ b/WebDriverAgentLib/Routing/FBTCPSocket.m
@@ -38,7 +38,7 @@
 
 - (BOOL)startWithError:(NSError **)error
 {
-  if (![self.listeningSocket acceptOnPort:self.port error:error]) {
+  if (![self.listeningSocket acceptOnInterface:@"localhost" port:self.port error:error]) {
     return NO;
   }
 
@@ -79,7 +79,7 @@
 {
   id<FBTCPSocketDelegate> delegate = self.delegate;
   if (nil != delegate) {
-    [delegate didClientSendData:sock];
+    [delegate didClient:sock sendData:data];
   }
 }
 
diff --git a/WebDriverAgentLib/Routing/FBWebServer.h b/WebDriverAgentLib/Routing/FBWebServer.h
index 9c2552d9a8e44e4f3a5cd55eac23dfb67e4dead0..e9a2f370d9f280bd437ceb8f90b254f78f5d764a 100644
--- a/WebDriverAgentLib/Routing/FBWebServer.h
+++ b/WebDriverAgentLib/Routing/FBWebServer.h
@@ -23,6 +23,9 @@ FOUNDATION_EXPORT BOOL FBRiviuRequestIsAuthorized(NSString *method,
                                                   NSString *path,
                                                   nullable NSString *providedToken,
                                                   NSData *expectedToken);
+FOUNDATION_EXPORT NSData *FBRiviuExpectedAgentToken(void);
+FOUNDATION_EXPORT BOOL FBRiviuAgentStreamIsReady(void);
+FOUNDATION_EXPORT void FBRiviuSetAgentStreamReady(BOOL ready);
 
 /**
  HTTP and USB service wrapper, handling requests and responses
diff --git a/WebDriverAgentLib/Routing/FBWebServer.m b/WebDriverAgentLib/Routing/FBWebServer.m
index 161836b9bc888396f5b12d780827e354dfe4a4d4..8418395af457c6277642acdd21645c5cfa2686b6 100644
--- a/WebDriverAgentLib/Routing/FBWebServer.m
+++ b/WebDriverAgentLib/Routing/FBWebServer.m
@@ -12,6 +12,7 @@
 #import "RoutingHTTPServer.h"
 #import "HTTPDataResponse.h"
 #import "HTTPMessage.h"
+#import <stdatomic.h>
 
 #import "FBCommandHandler.h"
 #import "FBErrorBuilder.h"
@@ -32,7 +33,8 @@ static NSString *const FBServerURLEndMarker = @"<-ServerURLHere";
 NSString *const FBRiviuAgentTokenEnvironmentName = @"RIVIU_AGENT_TOKEN";
 NSString *const FBRiviuAgentTokenHeaderName = @"X-Riviu-Token";
 
-static NSData *FBRiviuAgentTokenData;
+static NSData *FBRiviuExpectedAgentTokenData;
+static volatile atomic_bool FBRiviuStreamReady = false;
 
 NSData *FBRiviuRequireAgentToken(NSDictionary<NSString *, id> *environment)
 {
@@ -83,6 +85,21 @@ BOOL FBRiviuRequestIsAuthorized(NSString *method,
   return FBRiviuConstantTimeDataEqual(expectedToken, providedData);
 }
 
+NSData *FBRiviuExpectedAgentToken(void)
+{
+  return FBRiviuExpectedAgentTokenData ?: NSData.data;
+}
+
+BOOL FBRiviuAgentStreamIsReady(void)
+{
+  return atomic_load(&FBRiviuStreamReady);
+}
+
+void FBRiviuSetAgentStreamReady(BOOL ready)
+{
+  atomic_store(&FBRiviuStreamReady, ready);
+}
+
 @interface FBRiviuUnauthorizedResponse : HTTPDataResponse
 @end
 
@@ -112,7 +129,7 @@ BOOL FBRiviuRequestIsAuthorized(NSString *method,
 {
   NSString *requestPath = request.url.path ?: path;
   NSString *providedToken = [request headerField:FBRiviuAgentTokenHeaderName];
-  if (!FBRiviuRequestIsAuthorized(method, requestPath, providedToken, FBRiviuAgentTokenData)) {
+  if (!FBRiviuRequestIsAuthorized(method, requestPath, providedToken, FBRiviuExpectedAgentToken())) {
     NSData *body = [@"{\"value\":{\"error\":\"unauthorized\",\"message\":\"Authentication required\",\"stacktrace\":\"\"}}"
                     dataUsingEncoding:NSUTF8StringEncoding];
     return [[FBRiviuUnauthorizedResponse alloc] initWithData:body];
@@ -179,7 +196,7 @@ BOOL FBRiviuRequestIsAuthorized(NSString *method,
 
 - (void)startHTTPServer
 {
-  FBRiviuAgentTokenData = FBRiviuRequireAgentToken(NSProcessInfo.processInfo.environment);
+  FBRiviuExpectedAgentTokenData = FBRiviuRequireAgentToken(NSProcessInfo.processInfo.environment);
   self.server = [[RoutingHTTPServer alloc] init];
   [self.server setRouteQueue:dispatch_get_main_queue()];
   [self.server setDefaultHeader:@"Server" value:@"WebDriverAgent/1.0"];
@@ -223,6 +240,7 @@ BOOL FBRiviuRequestIsAuthorized(NSString *method,
 
 - (void)initScreenshotsBroadcaster
 {
+  FBRiviuSetAgentStreamReady(NO);
   [self readMjpegSettingsFromEnv];
   self.mjpegServer = [[FBMjpegServer alloc] init];
   self.screenshotsBroadcaster = [[FBTCPSocket alloc]
@@ -234,11 +252,14 @@ BOOL FBRiviuRequestIsAuthorized(NSString *method,
     [self.mjpegServer stopStreaming];
     self.mjpegServer = nil;
     self.screenshotsBroadcaster = nil;
+    return;
   }
+  FBRiviuSetAgentStreamReady(YES);
 }
 
 - (void)stopScreenshotsBroadcaster
 {
+  FBRiviuSetAgentStreamReady(NO);
   if (nil == self.screenshotsBroadcaster) {
     self.mjpegServer = nil;
     return;
diff --git a/WebDriverAgentLib/Utilities/FBMjpegServer.m b/WebDriverAgentLib/Utilities/FBMjpegServer.m
index 6d625042282b0861f216090ff4c96e758ff81c43..9889c9e01fac8619eeb6f3d5a64ae01d4b2bd4d4 100644
--- a/WebDriverAgentLib/Utilities/FBMjpegServer.m
+++ b/WebDriverAgentLib/Utilities/FBMjpegServer.m
@@ -14,6 +14,7 @@
 #import "GCDAsyncSocket.h"
 #import "FBConfiguration.h"
 #import "FBLogger.h"
+#import "FBWebServer.h"
 #import "FBScreenshot.h"
 #import "FBImageProcessor.h"
 #import "FBImageUtils.h"
@@ -26,12 +27,38 @@ static const NSTimeInterval FAILURE_BACKOFF_MAX = 10.0;
 
 static NSString *const SERVER_NAME = @"WDA MJPEG Server";
 static const char *QUEUE_NAME = "JPEG Screenshots Provider Queue";
+static const NSTimeInterval CLIENT_HEADER_TIMEOUT = 5.0;
+static const NSUInteger CLIENT_HEADER_MAX_BYTES = 8192;
 
 static NSUInteger FBNormalizedMjpegFramerate(NSUInteger framerate)
 {
   return (0 == framerate || framerate > MAX_FPS) ? MAX_FPS : framerate;
 }
 
+static NSString *FBMjpegHeaderValue(NSData *requestData, NSString *headerName)
+{
+  NSString *requestText = [[NSString alloc] initWithData:requestData
+                                                 encoding:NSUTF8StringEncoding];
+  if (nil == requestText) {
+    return nil;
+  }
+  NSArray<NSString *> *lines = [requestText componentsSeparatedByString:@"\r\n"];
+  for (NSString *line in lines) {
+    NSRange separator = [line rangeOfString:@":"];
+    if (NSNotFound == separator.location) {
+      continue;
+    }
+    NSString *name = [[line substringToIndex:separator.location]
+                      stringByTrimmingCharactersInSet:NSCharacterSet.whitespaceCharacterSet];
+    if ([name caseInsensitiveCompare:headerName] != NSOrderedSame) {
+      continue;
+    }
+    return [[line substringFromIndex:separator.location + 1]
+            stringByTrimmingCharactersInSet:NSCharacterSet.whitespaceCharacterSet];
+  }
+  return nil;
+}
+
 
 @interface FBMjpegServer()
 
@@ -167,11 +194,14 @@ static NSUInteger FBNormalizedMjpegFramerate(NSUInteger framerate)
 - (void)didClientConnect:(GCDAsyncSocket *)newClient
 {
   [FBLogger logFmt:@"Got screenshots broadcast client connection at %@:%d", newClient.connectedHost, newClient.connectedPort];
-  // Start broadcast only after there is any data from the client
-  [newClient readDataWithTimeout:-1 tag:0];
+  NSData *headerTerminator = [@"\r\n\r\n" dataUsingEncoding:NSUTF8StringEncoding];
+  [newClient readDataToData:headerTerminator
+                withTimeout:CLIENT_HEADER_TIMEOUT
+                  maxLength:CLIENT_HEADER_MAX_BYTES
+                        tag:0];
 }
 
-- (void)didClientSendData:(GCDAsyncSocket *)client
+- (void)didClient:(GCDAsyncSocket *)client sendData:(NSData *)data
 {
   @synchronized (self.listeningClients) {
     if ([self.listeningClients containsObject:client]) {
@@ -179,6 +209,18 @@ static NSUInteger FBNormalizedMjpegFramerate(NSUInteger framerate)
     }
   }
 
+  NSString *requestText = [[NSString alloc] initWithData:data encoding:NSUTF8StringEncoding];
+  NSString *providedToken = FBMjpegHeaderValue(data, FBRiviuAgentTokenHeaderName);
+  BOOL validRequestLine = [requestText hasPrefix:@"GET / HTTP/"];
+  if (!validRequestLine ||
+      !FBRiviuRequestIsAuthorized(@"GET", @"/mjpeg", providedToken, FBRiviuExpectedAgentToken())) {
+    NSData *unauthorized = [@"HTTP/1.0 401 Unauthorized\r\nConnection: close\r\nContent-Length: 0\r\n\r\n"
+                            dataUsingEncoding:NSUTF8StringEncoding];
+    [client writeData:unauthorized withTimeout:1.0 tag:0];
+    [client disconnectAfterWriting];
+    return;
+  }
+
   [FBLogger logFmt:@"Starting screenshots broadcast for the client at %@:%d", client.connectedHost, client.connectedPort];
   NSString *streamHeader = [NSString stringWithFormat:@"HTTP/1.0 200 OK\r\nServer: %@\r\nConnection: close\r\nMax-Age: 0\r\nExpires: 0\r\nCache-Control: no-cache, private\r\nPragma: no-cache\r\nContent-Type: multipart/x-mixed-replace; boundary=--BoundaryString\r\n\r\n", SERVER_NAME];
   [client writeData:(id)[streamHeader dataUsingEncoding:NSUTF8StringEncoding] withTimeout:-1 tag:0];
diff --git a/WebDriverAgentTests/UnitTests/FBRouteTests.m b/WebDriverAgentTests/UnitTests/FBRouteTests.m
index 9a24d260fd0a43cec235f04239479771ab843194..db1b52067d5d0d1ee4f1e3a41500db6dd19a593c 100644
--- a/WebDriverAgentTests/UnitTests/FBRouteTests.m
+++ b/WebDriverAgentTests/UnitTests/FBRouteTests.m
@@ -163,6 +163,7 @@
 
 - (void)testRiviuIdentityPayload
 {
+  FBRiviuSetAgentStreamReady(YES);
   NSDictionary<NSString *, id> *identity = FBSessionCommands.riviuAgentIdentity;
   XCTAssertEqualObjects(identity[@"agentVersion"], @"0.1.0");
   XCTAssertEqualObjects(identity[@"protocolVersion"], @2);
@@ -170,6 +171,15 @@
   XCTAssertEqualObjects(identity[@"logicalWidth"], @375);
   XCTAssertEqualObjects(identity[@"logicalHeight"], @667);
   XCTAssertEqualObjects(identity[@"state"], @"ready");
+  FBRiviuSetAgentStreamReady(NO);
+}
+
+- (void)testRiviuIdentityReportsDegradedWhenStreamDidNotBind
+{
+  FBRiviuSetAgentStreamReady(NO);
+  NSDictionary<NSString *, id> *identity = FBSessionCommands.riviuAgentIdentity;
+  XCTAssertEqualObjects(identity[@"features"], (@[@"tap", @"swipe", @"clipboard"]));
+  XCTAssertEqualObjects(identity[@"state"], @"degraded");
 }
 
 - (void)testRiviuNativeGestureRoutesDoNotRequireSession
