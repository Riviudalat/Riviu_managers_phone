# Dọn tài liệu ngày 06/09/2026

## Phạm vi và bảo toàn

Baseline của điều phối nằm trong `target/system-ui-review-20260906/BASELINE.tar`.
Worktree trước khảo sát sạch; không sửa root AGENTS, không đụng stash, registered
worktree hay dữ liệu vận hành. Không xoá `target/`, `.superpowers/`, thư viện sidecar,
ảnh E2E/detector, fonts hoặc asset branding đang dùng.

Kho ban đầu có 1.559 file tracked, 139 file dưới docs (59 Markdown), 17 script dưới
scripts. Mọi script có ít nhất một reference theo basename; không có script bị xoá.

## Di chuyển

[path-map.json](path-map.json) liệt kê 29 old/new paths: năm báo cáo ở gốc docs,
16 kế hoạch + README và bảy specs. Giữ nội dung lịch sử, cập nhật reference trong code,
skill và Markdown đang dùng. Các patch/diff và JSON chứng cứ giữ nguyên byte; đọc
chúng cùng path map. Không đổi số § hay tên topic agent để làm đẹp đường dẫn.
Đối chiếu 29/29 file với `git show 2c747826:<old-path>` sau đúng các phép đổi đường dẫn
khai báo cho kết quả khớp toàn bộ dòng (chuẩn hoá CRLF/LF), exit 0.

`diary/06-2408-2708.md` được giữ tên vì root AGENTS trỏ gián tiếp qua index hiện có;
index hiện hiển thị mục mới nhất, không dùng khoảng ngày trong filename làm trạng thái.
Mục mới vẫn thêm số mới. Có thể tách diary sau bằng migration riêng giữ toàn bộ anchors.

## Xoá đã chứng minh

[deletions-2026-09-06.json](deletions-2026-09-06.json) lưu từng path, size, SHA-256 và
quy tắc tái tạo. Tổng: 17 file, 747.148 byte.

- Năm scaffold không có reference tracked: `src/assets/hero.png`, `react.svg`,
  `vite.svg`, `public/favicon.svg`, `public/icons.svg` trong app desktop. Favicon
  thực dùng `/logo.jpg`; dynamic glob chỉ dùng fonts, source và test.
- Mười hai `.{before,drawer,armed,sent}.framed` trong ba bộ ảnh comment bằng đúng
  4 byte big-endian độ dài + sibling JPEG. Đã so kích thước và SHA-256 12/12 trước
  khi xoá; JPEG và report JSON đều giữ lại. Tổng phần trùng là 706.678 byte.

`scripts/check_docs.py` tái tạo bytes trong bộ nhớ rồi đối chiếu hash manifest ở mỗi
lần chạy, đồng thời chặn artifact xoá xuất hiện trở lại. Nó không chạy lại thao tác
thiết bị. `app-icon.jpg` giữ lại làm nguồn branding; hash trùng chưa đủ lý do xoá.

## Chỉ mục và cổng

Parser cũ báo `index up to date (238 sections)` dù có tám mục diary giả, mất 53 mục
con và nhận `5.2b` thành `5`. Parser mới giữ suffix, phân biệt diary observation với
section, giữ tên checkpoint 9/10, liệt kê tất cả mục con và slug heading GFM.
Các va chạm lịch sử có ngoại lệ đếm rõ; va chạm mới làm cổng đỏ.

Scope được lấy từ `git ls-files`, không từ cây thư mục có ignored worktree. Link
Markdown phải trỏ tới file tracked/anchor thật. File mới phải được đưa vào Git trước
cổng cuối. Runtime skill ignored không làm chứng cho nguồn `.claude/skills`.

Baseline regression: `python -X utf8 -B -m unittest scripts.test_build_agents_index -q`
đỏ đúng 10 case (một anchor sai, chín API parser chưa có), exit 1. Sau sửa, cùng bộ
đạt 10/10; thêm link tests đạt 15/15, exit 0. Full Python lần đầu có một lỗi CSV chỉ do
CRLF/LF; test được đổi sang so `csv.reader` rows, vẫn bắt cell thay đổi. Không đổi byte
ma trận provenance. Full lần cuối đạt 174 test, một platform skip; Gate 0 đạt 37/37.
Android tools, version 0.2.3, docs index 231 mục, docs link 83 Markdown và cargo deny
đều exit 0. Log nguyên văn, command/input/exit ở `target/system-ui-review-20260906/`
với tên `*-final.json`; lỗi baseline giữ ở `python-suite.json`. Rust docs gates do
điều phối chạy cùng suite Rust, không lấy kết quả Python thay bằng chứng backend/live.

```powershell
python -m unittest scripts.test_build_agents_index scripts.test_check_docs -v
python scripts/build_agents_index.py --check
python scripts/check_docs.py
cargo test -p riviu-managers-phone every_agents_section_citation_resolves --lib --locked
```
