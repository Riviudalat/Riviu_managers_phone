# Dọn workspace ngày 10/09/2026

Thực hiện theo yêu cầu commit, push và dọn thư mục sau khi nghiệm thu bản 0.2.24.
Commit gồm toàn bộ mã nguồn, fixture, test và tài liệu của đợt sửa publish,
hẹn giờ, stream phóng to, Sheet và đóng gói Windows. Bản release đã kiểm chứng
core 1517 pass/1 ignored, desktop 351 pass/3 ignored; core/desktop Clippy đều đạt.
Không thay đổi runtime trong bước dọn; 860 file của snapshot release vẫn khớp hash.

942 file ảnh chụp, trace và UI thử từ `.playwright-cli/` và `output/` được nén vào
`target/local-evidence-archive-20260910/ui-evidence.zip`. Từng file được đọc lại
trong ZIP và đối chiếu SHA-256 trước khi xoá bản rời. `path-map.json` cạnh ZIP ghi
đường dẫn cũ và archive entry, để tra các đường dẫn trong nhật ký lịch sử.
Archive có SHA-256 `88fc9e7e0b7dfa5bb214344b6cc05506131bf46309984f70a05468fd32f0cb3f`.

Chỉ dọn `deps`, `build`, `.fingerprint`, `examples`, `incremental` thuộc cache
Rust `target/debug` và những target test cũ có `.rustc_info.json`. Danh sách tuyệt
đối và dung lượng trước/sau nằm trong `target/commit-cleanup-20260910/`.
Trước khi xoá, kiểm tra biên workspace, reparse points, Git và executable đang
chạy. Cache được tạo lại bằng Cargo khi build/test; bản dev đang chạy nằm trong
`target/publish-stage-fix-20260910/run` nên không bị dọn.

Giữ toàn bộ source/test fixtures, ảnh snapshot versioned, sidecar, dependencies,
Git worktree/stash, cơ sở dữ liệu, báo cáo nghiệm thu, rollback và thư mục bộ cài
`C:/Users/cattfan/Desktop/Riviu_Manager_0.2.24`. Không dùng `git clean -xfd` hay
xoá toàn bộ `target/`.

Thêm ignore cho `/.playwright-cli/` và `/output/`. Probe `git check-ignore` trên
bản sao trả exit1 trước sửa, exit0 sau sửa, exit1 sau rollback; file sửa thật giữ
nguyên. Cổng cuối gồm staged diff check, index, docs, đối chiếu snapshot release
và xác nhận commit local trùng `origin/main` sau push.
