# Riviu Agent Gate B

Status: `PENDING_MAC_DEVICE`

Windows has verified the pinned source overlay, control and MJPEG auth boundaries,
dynamic ready/degraded identity, candidate ports, build orchestration, artifact
attestation for source and xcconfig from typed keys in the embedded signed XCTest
bundle, Xcode 26 runtime-closure enforcement and deep re-verification, token preflight
covering the manifest, IPA, source, locked xcconfig and argv,
transactional report publication, and fixture lifecycle. Pending evidence is a fresh install of the
manifest-bound IPA followed by five DVT plain-launch cycles with protected health,
fresh automation sessions, session-scoped commands, and the first decoded JPEG
after each session. Every cycle must also prove the prior process is absent, both
ports are closed, and the launched PID is new, still identical after the first
JPEG, and is the PID terminated before the following cycle.
The final cycle applies the same equality check during cleanup.

HTTP liveness alone is not a Gate B result.
